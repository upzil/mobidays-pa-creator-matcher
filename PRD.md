# Creator Match PRD

## 1. 문서 정보

- 제품명: Creator Match
- 형태: 브라우저 기반 크리에이터 추천 프로토타입
- 대상: 인플루언서 캠페인을 기획하는 광고주 또는 미디어 플래너
- 상태: 핵심 범위 구현 및 검증 완료

## 2. 배경과 문제

광고주는 캠페인에 적합한 크리에이터를 찾을 때 예산, 콘텐츠 카테고리, 채널 규모를 동시에 고려해야 합니다. 후보가 많고 지표 단위가 서로 달라 단순 필터만으로는 우선순위를 정하기 어렵습니다. 팔로워 수가 크다는 사실만으로 실제 도달, 참여, 협업 만족도를 보장할 수도 없습니다.

이 프로토타입은 제한된 데이터 안에서 후보군을 안전하게 좁히고, 여러 성과 지표를 비교 가능한 점수로 변환하여 추천 근거와 함께 제시합니다.

## 3. 목표

- 사용자가 캠페인 목적과 세 가지 핵심 조건으로 적합한 후보를 빠르게 찾게 합니다.
- 팔로워 수 외의 성과·협업 지표를 함께 고려합니다.
- 추천 결과가 나온 이유를 사용자가 이해하게 합니다.
- 데이터가 없거나 조건에 맞는 후보가 없어도 다음 행동을 제안합니다.
- 추천 정책을 코드, UI, 문서에서 일관되게 유지합니다.

## 4. 비목표

- 실제 캠페인 계약 또는 결제
- 크리에이터 연락과 섭외
- 계정·권한·데이터 저장
- 외부 데이터 수집 또는 실시간 지표 갱신
- 머신러닝 모델 학습
- 완전한 성과 예측

## 5. 사용자

### 주요 사용자

크리에이터 캠페인 경험이 많지 않거나, 짧은 시간 안에 초기 후보군을 만들어야 하는 광고주·플래너입니다.

### 사용자 스토리

> 광고주로서 필요한 후보 수를 정하고, 캠페인 목적에 따라 필요할 때만 예산·제품 카테고리·채널 규모를 좁혀 추천 이유가 있는 1차 검토 후보를 빠르게 만들고 싶다.

## 6. 핵심 사용자 흐름

1. 시스템이 데이터를 검증하고 팔로워 많은 순의 전체 크리에이터 목록을 표시합니다.
2. 사용자는 추천 전에도 조회수·참여율·평점·협업비 기준으로 전체 목록을 탐색할 수 있습니다.
3. 캠페인 목적은 기본값 `균형 탐색`으로 시작하며 필요하면 노출 확대·참여 유도·전환 중심으로 바꿉니다.
4. 협업을 원하는 크리에이터 수를 입력합니다. 기본값은 5명이며 1~20명까지 허용합니다.
5. 필요하면 크리에이터 1인당 최대 예산, 카테고리, 나노·마이크로·매크로 규모를 선택합니다.
6. 선택하지 않은 필터는 전체 범위로 적용됩니다.
7. 추천을 요청합니다.
8. 시스템이 입력을 검증하고 정확히 일치하는 후보를 필터링해 목적별 가중치로 점수를 계산합니다.
9. 추천 후보를 상단 상세 영역으로 재구성하고 적용 목적·가중치와 후보별 추천 근거를 표시합니다.
10. 추천되지 않은 나머지 크리에이터는 아래 전체 목록에 유지합니다.
11. 필요하면 추천 결과 또는 나머지 전체 목록의 정렬을 바꾸거나 입력 조건을 수정합니다.
12. 정확한 후보가 없으면 시스템이 완화된 조건과 대안을 설명합니다.

## 7. 기능 요구사항

### FR-1. 캠페인 목적 선택

- 노출 확대·참여 유도·전환 중심·균형 탐색 중 하나를 사용하며, 별도 선택 없이도 추천할 수 있도록 `균형 탐색`을 기본값으로 제공합니다.
- 선택한 목적은 후보 필터가 아니라 추천 점수의 지표별 비중을 결정합니다.
- 추천 결과에 적용 목적, 목적별 설명, 정확한 가중치를 표시합니다.
- 목적을 바꾸면 기존 결과를 즉시 변경하지 않고 다시 추천할 때 새 가중치를 적용합니다.

| 캠페인 목적 | 참여율 | 평균 조회수 | 광고주 평점 | 캠페인 경험 | 예산 효율 |
|---|---:|---:|---:|---:|---:|
| 노출 확대 | 20% | 45% | 15% | 10% | 10% |
| 참여 유도 | 45% | 20% | 15% | 10% | 10% |
| 전환 중심 | 25% | 10% | 30% | 20% | 15% |
| 균형 탐색 | 30% | 25% | 20% | 15% | 10% |

직접 전환 데이터가 없으므로 `전환 중심`은 광고주 평점·참여율·캠페인 경험·예산 효율을 전환 가능성의 대리 지표로 사용합니다. 실제 전환 예측으로 오해하지 않도록 이 제한을 결과에 표시합니다.

### FR-2. 예산 입력

- 숫자만 입력할 수 있어야 합니다.
- 입력은 선택 사항이며, 비워두면 비용으로 후보를 제외하지 않습니다.
- 값을 입력한 경우 0원 이하는 제출할 수 없어야 합니다.
- 사용자가 읽기 쉬운 원화 형식을 함께 제공합니다.
- 예산은 크리에이터 1명과 협업할 때 사용할 수 있는 최대 예상 금액입니다.

### FR-3. 카테고리 선택

- 제공 데이터의 10개 카테고리 중 여러 개를 선택할 수 있어야 합니다.
- 복수 선택은 OR 조건으로 적용합니다.
- 하나도 선택하지 않으면 전체 카테고리를 대상으로 합니다.

### FR-4. 팔로워 규모 선택

- 나노: 10,000명 미만
- 마이크로: 10,000~99,999명
- 매크로: 100,000명 이상
- 한 번에 하나의 구간을 선택합니다.
- 선택하지 않으면 전체 규모를 대상으로 하며, 각 후보의 실제 규모에서 성과 지표를 정규화합니다.

### FR-4A. 추천 인원 입력

- 협업을 원하는 크리에이터 수를 1~20명의 정수로 입력합니다.
- 기본값은 5명입니다.
- 추천 등급의 우선순위를 유지한 채 모든 추천 섹션의 합계를 입력한 인원으로 제한합니다.
- 범위를 벗어나거나 비어 있으면 오류를 표시하고 추천 인원 입력으로 초점을 옮깁니다.

### FR-5. 데이터 파싱과 검증

- 제공된 CSV 원본을 변경하지 않습니다.
- 필수 열과 숫자 필드를 검증합니다.
- 필수 헤더 누락·중복, 파일 로드 실패, 유효 행 0건은 전체 오류로 처리합니다.
- 개별 필드 오류, 중복 ID, 캠페인 이력 교차 조건 위반은 해당 행만 제외하고 진단 정보에 행 번호와 원인을 남깁니다.
- 누락 평점과 캠페인 이력 0건을 명시적으로 다룹니다.

| 필드 | 도메인 타입 | 검증 및 결측 정책 |
|---|---|---|
| `creator_id` | `string` | trim 후 비어 있으면 제외. 중복이면 최초 행만 유지 |
| `creator_name` | `string` | trim 후 비어 있으면 제외. 이름 중복은 허용 |
| `category` | `string` | trim 후 비어 있으면 제외 |
| `platform` | `string` | trim 후 비어 있으면 제외. 표시용이며 필터에는 미사용 |
| `followers` | 양의 안전한 정수 | `> 0`; 소수·음수·NaN이면 제외 |
| `avg_view_count` | 0 이상의 안전한 정수 | `>= 0` |
| `engagement_rate` | 유한 숫자 | 백분율 단위, `0 <= x <= 100` |
| `total_campaign_count` | 0 이상의 안전한 정수 | `>= 0` |
| `total_campaign_budget_krw` | 0 이상의 안전한 정수 | `>= 0`; 평균비 계산에는 사용하지 않음 |
| `avg_campaign_budget_krw` | `number \| null` | 이력 0건이며 원본 0원이면 `null`; 이력 보유 시 양수 필수 |
| `advertiser_rating` | `number \| null` | 공란은 이력 0건일 때만 허용; 값은 1~5 |

캠페인 0건은 총예산 0원, 평균예산 0원, 평점 공란이어야 합니다. 이력 보유 행은 총예산·평균예산이 양수이고 평점이 있어야 합니다. `avg_campaign_budget_krw`는 총예산/횟수로 재계산하지 않고 원본 필드를 권위값으로 사용합니다.

### FR-6. 후보 선택

- 카테고리를 선택했을 때만 선택한 값 중 하나와 일치해야 합니다.
- 팔로워 구간을 선택했을 때만 해당 구간과 일치해야 합니다.
- 예산을 입력했을 때만 알려진 평균 캠페인 예산이 입력값 이하인 후보를 정확 일치로 분류하고, 비용 미상 후보를 탐색 후보로 구분합니다.
- 선택하지 않은 필터는 후보 제외 조건으로 사용하지 않습니다.

### FR-7. 추천 점수

점수는 FR-1의 선택 목적에 해당하는 가중치를 사용하며 모든 가중치의 합은 1입니다. `균형 탐색`은 참여율 30%, 평균 조회수 25%, 광고주 평점 20%, 캠페인 경험 15%, 예산 효율 10%인 기존 기본 정책입니다.

참여율과 평균 조회수는 쿼리 후보가 아니라 전체 유효 데이터의 동일 팔로워 구간을 모집단으로 사용합니다. 구간의 행 수를 `N`, 값 `x`보다 작은 행 수를 `L`, 같은 행 수를 `E`라 하면 평균순위 백분위는 다음과 같습니다.

```text
averageRank = L + (E + 1) / 2
percentile = N === 1 ? 0.5 : (averageRank - 1) / (N - 1)
```

동률은 같은 백분위를 받습니다. 광고주 평점은 유효 평점 전역 평균 `mu = 4.415028901734104`, 사전 강도 5건으로 보정합니다.

```text
adjustedRating = (campaignCount * rating + 5 * mu) / (campaignCount + 5)
```

보정 평점은 동일 구간의 이력 보유 후보끼리 평균순위 백분위로 변환합니다. 이력 없는 후보의 평점 기여도는 중립값 `0.5`이며 실제 평점처럼 표시하지 않습니다.

```text
experience = log(1 + campaignCount) / log(1 + segmentMaxCampaignCount)
budgetEfficiency = budget / (budget + knownAverageCost)
```

구간 전체의 캠페인 수가 0이면 경험 점수는 0입니다. 비용 미상 후보 또는 예산을 입력하지 않은 쿼리의 예산 기여도는 중립값 `0.5`입니다. 예산을 입력했을 때 비용이 예산과 같으면 예산 효율은 `0.5`이고, 비용이 낮을수록 1에 가까워집니다.

```text
rawScore = 100 * (
  wEngagement * engagementPercentile
  + wViews * viewPercentile
  + wRating * ratingPercentile
  + wExperience * experience
  + wBudget * budgetEfficiency
)
```

정렬은 반올림 전 `rawScore`로 수행하고 UI에서만 소수점 첫째 자리로 표시합니다. 원점수 동률은 참여율 내림차순, 평균 조회수 내림차순, `creator_id` 오름차순으로 결정합니다.

#### 실제 데이터 계산 사례

| 조건 | 결과 | 핵심 검산 |
|---|---|---|
| 균형 탐색·게임·나노·300,000원 | 정확 일치 | C0071 78.9점 > C0080 65.2점 > C0165 46.2점 |
| 균형 탐색·패션·나노·100,000원 | 탐색 후보 | C0011은 이력 0건으로 비용·평점 `null`, 점수 45.7, 견적 확인 경고 |
| 균형 탐색·게임·나노·220,000원 | 예산 완화 | 264,000원 이하의 C0071·C0080·C0165, 각 9.1%·18.2%·18.2% 초과 |
| 균형 탐색·게임·매크로·600,000원 | 규모 완화 | 인접 마이크로의 C0002 52.4점, C0014 48.0점 |

### FR-8. 추천 근거

- 각 후보에 2~3개의 핵심 근거를 제공합니다.
- 근거는 실제 계산된 지표와 조건에서 생성합니다.
- 예: 동일 규모 참여율 상위 10%, 입력 예산 이내, 광고주 평점 4.8점
- 데이터가 부족하면 장점처럼 포장하지 않고 확인 필요 사항으로 표시합니다.

### FR-9. 빈 결과와 조건 완화

정확한 후보가 없을 때 다음 순서로 대안을 제공합니다.

1. 어떤 조건에서 후보가 사라졌는지 설명
2. 같은 카테고리·규모에서 예산의 120% 이내 후보
3. 같은 카테고리의 인접 팔로워 규모이면서 예산 이내 후보
4. 같은 카테고리·규모의 비용 미상 탐색 후보
5. 예산 증가, 규모 변경, 카테고리 수정 행동

완화된 조건은 자동으로 원래 입력을 바꾸지 않으며 결과에 명시합니다.

- 정확 후보가 있으면 정확 후보를 주 결과로, 같은 카테고리·규모의 이력 없는 후보를 별도 탐색 섹션으로 표시합니다. 예산·규모 완화는 표시하지 않습니다.
- 정확 후보가 없으면 `예산 완화 → 규모 완화 → 탐색 후보 → 완전 빈 결과` 중 처음으로 비어 있지 않은 한 단계만 표시합니다.
- 예산 완화는 `budget < cost <= 1.2 * budget`이며 구현에서는 정수 비교 `cost * 5 <= budget * 6`을 사용합니다.
- 인접 규모는 나노→마이크로, 매크로→마이크로입니다. 마이크로는 나노와 매크로를 모두 같은 거리의 인접 구간으로 취급합니다.
- 규모 완화 후보는 후보 자신의 실제 구간을 기준으로 점수를 계산합니다.
- 서로 다른 후보 등급을 하나의 순위로 섞지 않으며 각 등급 안에서만 점수순으로 정렬합니다.
- 최종 노출은 등급 우선순위를 유지하면서 사용자가 입력한 추천 인원까지로 제한합니다.

### FR-10. 결과 정렬

추천 전 전체 목록:

- 팔로워 많은 순을 기본값으로 사용합니다.
- 평균 조회수 많은 순, 참여율 높은 순, 광고주 평점 높은 순, 평균 협업비 낮은 순을 제공합니다.
- 200명 전체를 유지한 채 선택한 지표로 순서만 변경합니다.

추천 실행 후 결과:

- 추천순을 기본값으로 사용합니다.
- 참여율순, 평균 조회수순, 광고주 평점순, 평균 협업비 낮은 순을 제공합니다.
- 추천 후보는 상단 상세 영역에 배치하고, 추천되지 않은 나머지 크리에이터는 아래 전체 목록에 유지합니다.
- 추천 결과가 없어도 전체 크리에이터 목록은 사라지지 않습니다.

평점·비용이 없는 후보는 해당 정렬에서 오해가 없도록 별도 규칙을 적용하고 설명합니다.

- 정렬은 각 등급 섹션 내부에만 적용합니다.
- 평점이 없는 후보는 평점순에서 마지막, 비용이 없는 후보는 협업비순에서 마지막에 둡니다.
- 값이 같거나 모두 미상이면 기본 추천순을 유지합니다.

## 8. UI 상태

- 최초 진입 시 팔로워순 전체 크리에이터 목록
- 균형 탐색·추천 인원 5명·나머지 필터 미선택인 초기 상태
- 예산·카테고리·규모를 모두 비운 전체 대상 추천 상태
- 추천 인원 범위 오류와 입력 초점 상태
- 적용한 캠페인 목적과 가중치가 표시된 추천 상태
- 목적을 변경한 뒤 다시 추천하면 점수와 순위가 갱신되는 상태
- 추천 후보가 상단으로 이동하고 나머지 전체 목록이 유지되는 재정렬 상태
- 유효한 입력
- 필드별 검증 오류
- 데이터 로딩
- 정확 일치 결과
- 탐색 후보 포함 결과
- 예산 또는 규모 완화 결과
- 대안도 없는 결과
- CSV 로드·파싱 오류
- 캠페인 이력이 없는 후보
- 결과 1명 또는 다수
- 모바일·태블릿·데스크톱

결과는 마지막으로 제출한 조건을 `appliedQuery`로 보존합니다. 폼을 편집해도 기존 결과를 즉시 재계산하지 않고 “조건이 변경됐어요. ‘크리에이터 추천받기’를 눌러 결과를 갱신해 주세요.”라고 알립니다. 새 제출이 성공한 뒤에만 적용 조건과 결과를 교체합니다.

결과 카드는 등급, 이름, 카테고리·플랫폼, 추천 점수, 팔로워, 평균 조회수, 참여율, 광고주 평점, 캠페인 횟수, 평균 협업비, 계산된 추천 이유 2~3개와 필요한 경고를 표시합니다. 평점 공란은 `이력 없음`, 비용 미상은 `견적 확인 필요`로 표시합니다.

모바일은 단일 열, 태블릿은 결과 2열, 1,200px 이상 데스크톱은 360~400px 조건 패널과 유동 결과 영역의 2열 구조를 사용합니다. 조건 패널은 네이티브 선택 상자, 숫자 입력, 펼침형 카테고리 다중 선택으로 면적을 줄입니다. 입력은 실제 label·fieldset·checkbox·select를 사용하고, 제출 오류 시 첫 오류로 초점을 옮기며, 결과 갱신과 정렬 변경은 polite live region으로 알립니다.

## 9. 비기능 요구사항

- 추천 결과는 동일 입력과 동일 데이터에서 항상 같아야 합니다.
- 추천 계산은 UI와 분리된 순수 함수여야 합니다.
- 모바일과 데스크톱에서 핵심 흐름을 사용할 수 있어야 합니다.
- 폼 라벨, 키보드 포커스, 오류 메시지, 상태 알림을 제공해야 합니다.
- 정적 데이터만 사용하므로 별도 서버나 API 키가 없어야 합니다.
- 관련 lint, typecheck, 테스트, production build가 통과해야 합니다.

## 10. 수용 기준

- 기본 균형 탐색과 추천 인원 5명만으로 전체 데이터에서 추천 목록을 생성할 수 있습니다.
- 예산·카테고리·규모는 각각 또는 모두 비워도 추천할 수 있습니다.
- 추천 인원은 1~20명만 허용하며 잘못된 값은 명확한 오류와 입력 초점을 제공합니다.
- 목적별 가중치의 합이 100%이며 선택한 목적에 따라 점수와 순위가 달라집니다.
- 적용한 목적과 정확한 가중치가 결과에 표시됩니다.
- 추천 전에도 전체 크리에이터를 확인하고 다섯 가지 기본 지표로 정렬할 수 있습니다.
- 다중 카테고리 선택이 OR 조건으로 동작합니다.
- 팔로워 구간 경계값이 정확하게 분류됩니다.
- 평점 공란이 0점으로 표시되지 않습니다.
- 이력 없는 후보의 평균 협업비가 무료로 표시되지 않습니다.
- 후보별 추천 이유가 계산 결과와 일치합니다.
- 후보가 없을 때 이유와 실행 가능한 대안이 표시됩니다.
- 모든 정렬이 안정적으로 동작합니다.
- 모바일과 데스크톱에서 사용할 수 있습니다.
- 필수 문서와 실제 구현의 기준이 일치합니다.

## 11. 분석 및 평가 지표

프로토타입이므로 외부 분석 도구는 추가하지 않습니다. 수동·자동 검증에서 다음을 확인합니다.

- 유효 입력의 추천 성공률
- 정확 결과와 fallback 결과 구분
- 점수의 결정론적 재현성
- 추천 이유와 원시 데이터의 일치
- 오류 상태에서 회복 행동의 제공 여부

## 12. 위험과 대응

| 위험 | 대응 |
|---|---|
| 평균 예산이 실제 견적과 다름 | 과거 평균값임을 표시하고 의사결정 지원 용도로 제한 |
| 신규 크리에이터 정보 부족 | 중립값과 낮은 신뢰도를 적용하고 탐색 후보로 분리 |
| 이상치가 점수를 지배 | 구간 내 백분위와 로그 변환 사용 |
| 조건 완화가 정확 추천처럼 보임 | 별도 섹션과 명시적인 완화 설명 사용 |
| 목적별 가중치가 주관적 | 네 가지 정책과 정확한 비중을 공개하고 목적별 순위 변화를 테스트 |
| 직접 전환 데이터가 없음 | 평점·참여율·경험·예산 효율을 대리 지표로 사용하며 예측 한계를 UI에 명시 |

## 13. 향후 확장

- 플랫폼별 기준 분리
- 실제 클릭·구매·전환율을 이용한 전환 목적 점수 보정
- 실제 견적과 성과를 통한 보정
- 복수 크리에이터 포트폴리오 추천
- 사용자 피드백 기반 랭킹 개선

## 14. 구현 및 검증 결과

- React 19, TypeScript, Vite 기반의 정적 단일 페이지 애플리케이션으로 구현했습니다.
- Papa Parse 기반 파서가 BOM, 필수·중복 헤더, 숫자 범위, 이력 교차 조건, 중복 ID를 검증합니다.
- 추천 엔진은 후보 배열과 조건을 입력받는 순수 함수이며 입력 데이터를 변경하지 않습니다.
- Vitest와 Testing Library 자동 테스트 30개가 데이터·전체 목록 정렬·선택형 필터·추천 인원 제한·목적별 가중치·추천·UI 계약을 검증합니다.
- ESLint, TypeScript 타입 검사, Vite production build가 통과했습니다.
- 데스크톱과 모바일 폭에서 컴팩트 조건 패널과 실제 추천 흐름을 확인했습니다. 필터를 모두 비운 기본 5명 추천, `2명·게임·나노·300,000원` 조합, 노출 확대와 전환 중심 전환에 따른 가중치·점수·핵심 근거 변화를 검증했습니다.
- 현재 범위는 로컬 실행 가능한 프로토타입입니다. 외부 배포, 사용자 계정, 데이터 저장은 비목표로 유지합니다.
