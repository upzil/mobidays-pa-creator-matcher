# AI 활용 기록

이 문서는 과제 진행 중 AI를 어떻게 활용했고, 결과를 어떤 기준으로 검토·수정했는지 누적 기록합니다. 구현이 진행될 때마다 실제 사용 내역을 추가합니다.

## 1. 사용 도구

### OpenAI Codex

사용 이유:

- 과제 안내문 구조화와 누락 없는 요구사항 추출
- CSV 데이터 프로파일링과 예외 상황 확인
- 제품 범위, 추천 정책, 작업 순서 설계
- 프로젝트 공통 지침과 전문 역할별 작업 분배
- 제출 문서 초안 작성
- 소스 구현, 자동 테스트, 브라우저 검증과 제출 문서 동기화

## 2. 제공한 컨텍스트

- 모비데이즈 P&A 사전 과제 안내 Notion 페이지
- 과제에서 제공한 `dummy_creators.csv`
- 프로젝트 루트의 `AGENTS.md`
- 전체 계획 `PROJECT_PLAN.md`
- 대화 간 진행 기록 `PROJECT_STATUS.md`

개인정보, 비밀키, 환경변수는 AI 컨텍스트로 제공하지 않았습니다.

## 3. 주요 프롬프트와 활용 목적

### 요구사항 파악

> 모비데이즈라는 회사에 지원해서 서류 합격했어. 과제전형을 진행해야 해. 아래 과제 안내 문서의 내용을 우선 완벽하게 파악해.

활용 결과:

- 필수 기능, 가산점, 제출 방식, 문서 요구사항을 분리했습니다.
- 단순 화면보다 추천 로직의 설계 근거와 문서화가 핵심 평가 대상임을 확인했습니다.
- 원본 CSV 유지, Null Safety, 빈 결과 처리, 팔로워 구간 직접 정의가 중요한 제약임을 확인했습니다.

### 전체 작업 계획

> 요구하는 최고 수준으로 이 과제를 수행하기 위한 작업 순서를 계획해.

활용 결과:

- 데이터 분석 → 추천 정책 → UX 상태 → 구현 → 검증 → 문서 → 제출 순서를 만들었습니다.
- 테스트 가능한 추천 엔진을 UI보다 먼저 설계하기로 결정했습니다.

### 범위 재검토

> 제한시간은 72시간이지만 나는 앞으로 7시간 동안 과제를 진행할 거야. 계획에 이 점을 반영해.

> 일단 시간은 신경쓰지 말고 방금 풀버전 계획을 사용하자. 빨리 작업하면 되니까.

활용 결과:

- 처음에는 7시간용 축소 계획을 만들었습니다.
- 이후 사용자의 판단에 따라 축소 계획을 폐기하고 품질 중심의 전체 계획으로 복귀했습니다.
- 시간 제한을 이유로 필수 문서, 추천 근거, 테스트, fallback을 제거하지 않기로 했습니다.

### 협업 구조 설계

> 아까 정리한 작업 순서를 프로젝트 내 대화들에서 공통적으로 참고할 수 있도록 프로젝트 작업 지침을 구성해. 작업 순서에 따른 최적의 대화 및 에이전트 작업 배분안도 함께 작성해.

활용 결과:

- 총괄, 데이터·추천, UX·프론트엔드, 문서·발표, QA·릴리스 역할을 분리했습니다.
- 파일 소유권과 단계별 인계 규칙을 정의했습니다.
- `AGENTS.md`, `PROJECT_PLAN.md`, `PROJECT_STATUS.md`를 공통 기준으로 구성했습니다.

## 4. AI 결과를 그대로 사용하지 않은 판단 사례

### 사례 1. 캠페인 이력이 없는 후보의 예산 처리

초기 추천안에서는 `avg_campaign_budget_krw <= 입력 예산`을 단순한 필수 조건으로 사용할 수 있었습니다. 그러나 데이터에서 캠페인 이력이 없는 27명의 평균 예산이 0원이라는 점을 확인했습니다. 이를 그대로 적용하면 신규 크리에이터가 모든 예산에 맞는 무료 후보처럼 취급되는 문제가 있습니다.

수정한 방향:

- 캠페인 0건의 평균 예산 0원은 `비용 미상`으로 해석합니다.
- 검증된 정확 일치 후보와 `탐색 후보`를 분리합니다.
- UI에서 비용 확인이 필요하다고 명시합니다.

### 사례 2. 누락 평점을 0점으로 치환하지 않음

평점 공란을 숫자 0으로 치환하면 신규 크리에이터가 실제로 낮은 평가를 받은 것처럼 보입니다.

수정한 방향:

- 누락 평점은 `캠페인 이력 없음`으로 표현합니다.
- 점수 계산에는 중립 사전값 또는 캠페인 수를 반영한 보정값을 사용합니다.
- 캠페인 경험 지표에서 정보 신뢰도의 차이를 별도로 반영합니다.

### 사례 3. 조건 완화 순서 수정

관련 없는 카테고리까지 추천하면 후보 수는 늘지만 광고주의 캠페인 적합성을 해칩니다.

수정한 방향:

- fallback에서도 카테고리를 유지합니다.
- 예산을 먼저 제한적으로 완화하고 이후 인접 팔로워 규모를 검토합니다.
- 모든 완화 사실을 결과 화면에 표시합니다.

## 5. 검증 방식

현재까지 수행한 검증:

- 과제 안내문을 처음부터 끝까지 직접 확인
- CSV의 행·열 수, 카테고리·플랫폼 분포, 숫자 범위 확인
- 중복 ID와 이름 확인
- 누락 평점과 캠페인 0건의 관계 확인
- 팔로워 구간별 후보 수 확인
- 문서 간 가중치·구간·fallback 정책 대조

구현 후 추가할 검증:

- 추천 함수 단위 테스트
- 경계값과 결측치 테스트
- lint와 TypeScript 검사
- production build
- 모바일·데스크톱 브라우저 확인
- 키보드와 접근성 확인
- 코드와 README·PRD·flowchart의 최종 일치 확인

## 6. AI 활용 원칙

- AI가 제안한 기준도 데이터와 사용자 경험으로 검증합니다.
- 설명할 수 없는 복잡한 알고리즘은 사용하지 않습니다.
- 실제로 하지 않은 작업이나 판단을 기록하지 않습니다.
- AI가 만든 코드와 문서는 테스트·수동 검토 후 반영합니다.
- 구현이 변경되면 관련 문서와 이 기록도 함께 갱신합니다.

## 7. 구현 이후 확인할 내용

- 배포를 진행한다면 배포 환경과 최종 공개 URL
- 실제 사용자 피드백에 따른 가중치·문구 변경 이력

## 8. 작업별 감사 로그

### AI-20260913-01 — 프로젝트 공용 소스 동기화

- 단계: 협업 기반 정리
- 실제 요청: “다른 대화에서 PROJECT_PLAN.md 등 프로젝트 파일에 접근하지 못하고 있어.”
- 제공 컨텍스트: 로컬 조정 문서, 제출 문서, 원본 CSV
- AI 제안: 로컬 파일 경로를 전달하는 대신 ChatGPT 프로젝트 Sources를 공통 읽기 기준으로 사용
- 판단: 채택. 프로젝트 대화는 로컬 Codex 디렉터리를 직접 읽지 못하므로 문서 7개와 CSV를 프로젝트 Sources에 업로드
- 검증: 프로젝트 소스 화면에서 8개 파일이 모두 표시되는지 직접 확인
- 영향: 프로젝트 대화 간 컨텍스트 일치. 저장소 파일 내용은 변경하지 않음

### AI-20260913-02 — 역할별 Phase 2–4 재검토

- 단계: 데이터·추천, UX, QA·문서
- 실제 요청: “PROJECT_PLAN.md에 따라 다음 계획을 이어서 실행해. 역할에 따라 서브 대화들을 적절히 운용하고, 내 판단이 필요한 부분이 있다면 물어봐.”
- 제공 컨텍스트: `AGENTS.md`, `PROJECT_PLAN.md`, `PROJECT_STATUS.md`, 제출 문서 4종, `dummy_creators.csv`
- AI 제안: 데이터·추천, UX, 문서, QA 역할을 읽기 전용으로 병렬 배분하고 총괄 대화에서 정책을 통합
- 판단: 채택. 여러 역할이 같은 파일을 동시에 수정하지 않도록 보고 전용으로 제한
- 검증: 각 보고서의 데이터 통계, 문서 충돌, UI 상태, 테스트 경계를 총괄 대화에서 원본과 대조
- 영향: `PRD.md`, `README.md`, `flowchart.md`의 구현 전 계약 구체화

### AI-20260913-03 — 추천 수식 확정

- 단계: Phase 2–3
- AI 최초 제안 및 쟁점: 한 역할 대화는 경험과 `B / cost`를 다시 백분위로 만드는 안과 구간별 평점 prior를 제안했고, 다른 검토는 경험을 구간 최대 대비 `log1p` 비율로, 예산을 `B / (B + cost)`로 직접 쓰는 안을 제안했습니다. 순위 지표에서는 로그 변환이 순서를 바꾸지 않고, `B / cost`의 구간 내 순위에서는 동일한 `B`가 상쇄되어 입력 예산의 의미가 점수에서 사라지는 문제가 있었습니다. 후보 등급 목록과 no-result fallback의 탐색 후보 순서도 충돌했습니다.
- 사람의 판단: 참여율·조회수·보정 평점에는 동일 규모 평균순위 백분위를, 경험에는 구간 최대 대비 `log1p` 비율을 적용하도록 분리했습니다. 표본이 작은 구간에 따라 prior가 달라지지 않도록 전역 평점 평균을 사용하고, 예산 자체가 효율에 반영되는 bounded 함수 `B / (B + cost)`를 채택했습니다. 정확 결과가 있으면 탐색 후보를 별도 보조 섹션으로, 없으면 예산 완화→규모 완화→탐색 순서의 첫 유효 단계만 사용합니다.
- 확정 수식: Bayesian 사전 강도 5건, 예산 효율 `budget / (budget + cost)`, 비용·평점 미상 중립 기여도 `0.5`
- 검증: 실제 CSV의 정확·탐색·예산 완화·규모 완화 사례를 수기로 재계산하고 문서에 기록
- 영향: `PRD.md`, `README.md`, `flowchart.md`

### AI-20260913-04 — 재현 가능한 데이터 프로파일러

- 단계: Phase 2
- AI 제안: 외부 패키지 없이 Node.js만으로 BOM과 CRLF를 처리하고 스키마·타입·결측·중복·분포를 다시 확인하는 스크립트 추가
- 판단: 채택. 앱 의존성 설치 승인과 무관하게 원본 데이터 검증을 재현할 수 있음
- 검증: `node scripts/profile-data.mjs` 실행으로 200행·11열, 10개 카테고리, 플랫폼·구간 분포, 결측 27건, 중복 0건을 확인. 저장소 CSV와 원본의 SHA-256 일치 확인
- 영향: `scripts/profile-data.mjs`, `public/data/dummy_creators.csv`

### AI-20260913-05 — Git 줄바꿈 정규화 수정

- 단계: Phase 2 저장소 무결성 검증
- 발견: 작업 파일의 SHA-256은 원본과 같았지만 첫 커밋 직후 Git blob을 다시 해시하자 `core.autocrlf=input` 때문에 CRLF가 LF로 정규화되어 값이 달랐음
- 판단: `public/data/dummy_creators.csv`를 `-text`로 지정해 줄바꿈 변환을 금지
- 검증: 현재 커밋의 CSV blob과 다운로드 원본을 각각 SHA-256으로 계산해 동일한지 재확인
- 영향: `.gitattributes`, `public/data/dummy_creators.csv`

### AI-20260913-06 — 추천 엔진과 CSV 파서 구현

- 단계: Phase 6–7
- AI 제안: 데이터 계약, 점수 계산, 후보 등급, fallback, 추천 근거를 UI와 분리된 순수 함수로 구현하고 경계·결측·무변형성 테스트를 함께 작성
- 판단: 채택. 복잡한 모델이나 외부 API 대신 문서에서 직접 재현할 수 있는 계산식을 유지
- 사람 검토 및 수정: 규모 완화 경고가 후보의 실제 규모가 아니라 요청 규모를 기준으로 설명되도록 요청 구간을 명시적으로 전달했습니다. 비용·평점 결측 정렬은 항상 마지막으로 두고, 추천순은 반올림 전 점수를 사용하도록 확인했습니다.
- 검증: 실제 CSV의 게임·나노 300,000원, 패션·나노 100,000원, 게임·나노 220,000원, 게임·매크로 600,000원 사례를 고정 테스트로 검산
- 영향: `src/data/**`, `src/recommendation/**`, `src/domain/types.ts`

### AI-20260913-07 — 접근 가능한 반응형 UI 구현

- 단계: Phase 8
- AI 제안: 입력과 결과를 좌우로 분리한 에디토리얼 데스크형 화면, 아이보리·잉크 기반 절제된 시각 체계, 최소한의 인디고 강조색 적용
- 판단: 채택. 평가자가 조건과 결과를 한 화면에서 비교하고 추천 근거를 빠르게 읽는 데 집중하도록 장식적 요소를 줄였습니다.
- 사람 검토 및 수정: 정렬 변경 후에도 숫자를 절대 순위처럼 읽지 않도록 접근성 이름을 `목록 순서`로 바꾸고, 탐색 후보 배지는 `비용 확인 필요`로 구체화했습니다. 데이터 로딩 중에는 제출 버튼뿐 아니라 모든 입력을 비활성화했습니다.
- 검증: 실제 브라우저에서 200명 데이터 로딩, 정확 추천 3명, 예산 완화 후보와 초과율, 390px 모바일 단일 열, 콘솔 경고·오류 없음 확인
- 영향: `src/App.tsx`, `src/components/**`, `src/styles.css`

### AI-20260913-08 — 테스트 실패와 의존성 위험 수정

- 단계: Phase 9
- 발견 1: UI 재시도 테스트가 이전 테스트의 mock 호출 수를 공유해 기능은 정상인데 실패했습니다.
- 수정 1: 각 테스트 전에 mock 상태를 초기화해 테스트 간 격리를 보장했습니다.
- 발견 2: 초기 Vitest 버전의 보안 감사에서 중간 수준 취약점이 보고됐습니다.
- 수정 2: 승인된 테스트 도구 범위 안에서 Vitest를 4.1.11로 올리고 전체 검사를 다시 수행했습니다.
- 검증: 자동 테스트 20개, ESLint, TypeScript 타입 검사, production build 통과. npm audit 취약점 0건
- 영향: `src/App.test.tsx`, `package.json`, `package-lock.json`

### AI-20260913-09 — 추천 전 전체 크리에이터 탐색

- 단계: 사용자 흐름 개선
- 실제 요청: 추천 버튼을 눌러야 목록이 나타나는 구조를 바꾸고, 추천 전에는 기본 정렬로 전체 크리에이터를 노출한 뒤 추천 실행 시 재정렬
- AI 제안: 추천 전에는 정보 밀도가 높은 행 목록으로 200명을 표시하고 팔로워를 기본값으로 조회수·참여율·평점·협업비 정렬을 제공하며, 추천 후에는 추천 후보를 상단 상세 영역으로 올리고 나머지 전체 목록을 아래에 유지
- 판단: 채택. 추천 로직과 fallback 계약은 유지하면서 초기 화면의 탐색 가능성과 데이터 신뢰감을 높였습니다.
- 사람 검토 및 수정: 이력 없는 평점과 비용은 전체 목록 정렬에서도 마지막에 두고 0점·무료로 표현하지 않았습니다. 추천 전 목록은 점수나 추천 배지를 표시하지 않아 단순 지표 정렬과 추천을 혼동하지 않게 했고, 추천 실행 뒤에도 추천되지 않은 크리에이터를 제거하지 않았습니다.
- 검증: 전체 목록 정렬 단위 테스트와 UI 전환 테스트를 추가하고 자동 테스트 23개, lint, 타입 검사, production build를 통과했습니다. 실제 브라우저에서 추천 전 200명과 추천 후 상단 추천 3명·나머지 197명이 함께 표시되는 것을 확인했습니다.
- 영향: `src/components/CreatorDirectory.tsx`, `src/recommendation/browse.ts`, `src/App.tsx`, `src/components/ResultsPanel.tsx`, 관련 테스트·문서

### AI-20260913-10 — 캠페인 목적별 가중치 추천

- 단계: 추천 맥락과 설명 가능성 개선
- 실제 요청: 캠페인 목적을 선택하고, 선택한 목적에 따라 각 속성의 가중치를 달리해 크리에이터를 정렬
- AI 제안: 노출 확대·참여 유도·전환 중심·균형 탐색의 네 가지 필수 선택지를 제공하고, 동일한 후보군에 목적별 고정 가중치를 적용해 점수와 순위를 재계산
- 판단: 채택. 기존 점수는 `균형 탐색`으로 보존하고 노출은 조회수 45%, 참여는 참여율 45%, 전환은 평점 30%를 가장 크게 반영했습니다. 제공 데이터에 직접 전환 지표가 없으므로 전환 중심은 평점·참여율·캠페인 경험·예산 효율을 대리 지표로 사용하며 이 한계를 화면과 문서에 공개했습니다.
- 사람 검토 및 수정: 추천 근거도 단순 정규화값이 아니라 선택 목적의 가중 기여도가 큰 지표를 우선 설명하도록 맞췄습니다. 목적을 바꾸는 즉시 과거 결과를 덮지 않고 다시 추천할 때만 적용해 기존 `appliedQuery` 계약을 유지했습니다.
- 검증: 모든 목적의 가중치 합과 목적별 순위 변화를 단위 테스트로 고정하고 자동 테스트 26개, lint, 타입 검사, production build를 통과했습니다. 실제 브라우저에서 동일 조건의 노출 확대와 전환 중심을 번갈아 적용해 목적·가중치·점수·핵심 근거가 함께 바뀌는 것을 확인했습니다.
- 영향: `src/recommendation/goals.ts`, `src/recommendation/engine.ts`, `src/domain/types.ts`, `src/components/CampaignForm.tsx`, `src/components/ResultsPanel.tsx`, `src/App.tsx`, 관련 테스트·스타일·문서

### AI-20260913-11 — 선택형 컴팩트 필터와 추천 인원 제한

- 단계: 조건 입력 UX와 결과 범위 개선
- 실제 요청: 협업을 원하는 크리에이터 수를 기본 5명으로 입력하게 하고, 모든 필터를 선택 사항으로 바꾸며, 선택 상자 등을 사용해 필터가 차지하는 면적을 줄이기
- AI 제안: 캠페인 목적은 `균형 탐색`, 추천 인원은 5명을 기본값으로 두고 1~20명만 허용했습니다. 예산·규모는 네이티브 선택형 입력, 카테고리는 닫힌 상태에서 선택 요약만 보이는 펼침형 다중 선택으로 구성했습니다.
- 판단: 채택. 사용자가 별도 설정 없이도 전체 데이터에서 추천받을 수 있고, 필요한 필터만 좁히는 흐름이 초기 탐색 목적에 더 적합합니다.
- 사람 검토 및 수정: 예산 미입력은 비용 제한을 제거하고 예산 기여도를 모든 후보에 중립값 `0.5`로 적용했습니다. 카테고리 미선택은 전체 카테고리, 규모 미선택은 전체 규모로 해석하되 성과 정규화는 각 후보의 실제 규모에서 수행했습니다. 추천 인원 제한은 정확·탐색·완화 등급의 우선순위를 깨지 않고 전체 섹션 합계에 적용했습니다.
- 검증: 선택 필터 미입력, 결과 수 제한, 등급 우선순위, 범위 오류와 초점 이동을 자동 테스트로 고정해 총 30개 테스트와 lint, 타입 검사, production build를 통과했습니다. 실제 브라우저에서 필터를 비운 기본 5명 추천과 `2명·게임·나노·300,000원` 조합의 적용 조건 및 결과 수를 확인했습니다.
- 영향: `src/App.tsx`, `src/components/CampaignForm.tsx`, `src/components/ResultsPanel.tsx`, `src/recommendation/engine.ts`, `src/domain/types.ts`, 관련 테스트·스타일·문서

### AI-20260913-12 — 만원 단위 총예산과 중복 결과 요약 제거

- 단계: 예산 의미와 결과 정보 구조 개선
- 실제 요청: 1인당 최대 예산을 총예산으로 바꾸고 만원 단위로 입력하게 하며, 결과 상단에서 필터와 중복되는 적용 조건 영역 제거
- AI 제안: 총예산 `T`를 요청 인원 `N`으로 나눈 `T / N`을 데이터의 1인당 평균 협업비와 비교하는 기준으로 사용하고, 입력 즉시 `총 150만원 · 1인당 약 30만원 기준`처럼 환산값을 안내했습니다.
- 판단: 채택. 원본 데이터가 크리에이터별 평균 협업비를 제공하므로 총예산을 그대로 비교하는 대신 요청 인원으로 균등 배분한 기준을 명시적으로 사용하는 편이 재현 가능하고 설명하기 쉽습니다.
- 사람 검토 및 수정: 만원 단위 입력은 내부에서 원 단위의 안전한 정수로 변환하고, 후보 분류·예산 효율·완화율에 동일한 1인당 환산 예산을 사용했습니다. 첨부 이미지의 `APPLIED CONDITIONS`, 후보 수 제목, 조건 반복 목록, 조건 수정 링크는 모두 제거하고 목적별 가중치와 추천 섹션부터 보이게 했습니다.
- 검증: 추천 인원에 따라 같은 총예산의 1인당 기준이 달라지는 단위 테스트를 추가하고 총 31개 테스트, lint, 타입 검사, production build 및 실제 브라우저 흐름을 확인했습니다.
- 영향: `src/domain/types.ts`, `src/recommendation/engine.ts`, `src/components/CampaignForm.tsx`, `src/components/ResultsPanel.tsx`, `src/App.tsx`, 관련 테스트·스타일·문서

### AI-20260913-13 — 총예산 조합 최적화와 선택형 추천 인원

- 단계: 추천 정책 고도화와 입력 UX 수정
- 실제 요청: 총예산을 인원수로 균등 분할하지 않고 예산 안에서 조건에 가장 적합한 크리에이터 조합을 구성하며, 추천 인원 기본값을 빈 값으로 바꾸고 미입력 시 인원수 제한 제거
- AI 제안: 비용 확인 가능한 후보를 대상으로 평균 협업비 합이 총예산 이하인 조합 중 목적별 반올림 전 원점수 합이 가장 높은 조합을 0/1 배낭 동적 계획법으로 선택했습니다. 추천 인원이 있으면 해당 개수의 조합만, 없으면 가능한 모든 개수의 조합을 비교합니다.
- 판단: 채택. 균등 분할은 고비용 고성과 후보와 저비용 효율 후보를 함께 구성할 가능성을 차단하지만, 조합 최적화는 총예산이라는 사용자의 실제 제약을 직접 지키면서 전체 적합도를 높입니다.
- 사람 검토 및 수정: Pareto frontier로 비용·점수 모두 열등한 중간 상태를 제거하고, 동점은 낮은 비용과 크리에이터 ID 순으로 결정했습니다. 예산 효율은 동일 규모 내 비용 백분위의 역값으로 바꾸고, 비용 미상 후보는 예산 충족 조합에 섞지 않았습니다. 기존 20% 예산 초과 fallback은 “예산 안에서” 요구와 충돌하므로 제거했습니다. 결과에는 조건을 반복하지 않고 조합 예상 비용과 잔여 예산만 표시합니다.
- 검증: 요청 인원이 있는 최적 조합, 인원 미지정 최적 조합, 엄격한 총예산 상한, 선택형 추천 인원, 결정론적 결과를 자동 테스트로 고정하고 총 32개 테스트, lint, 타입 검사, production build 및 실제 브라우저 흐름을 확인했습니다.
- 영향: `src/recommendation/engine.ts`, `src/domain/types.ts`, `src/components/CampaignForm.tsx`, `src/components/ResultsPanel.tsx`, `src/components/CreatorCard.tsx`, `src/App.tsx`, 관련 테스트·스타일·문서

### AI-20260913-14 — 목적 가중치 슬라이더와 조건 패널 재구성

- 단계: 추천 제어 방식과 화면 정보 구조 개선
- 실제 요청: 캠페인 목적을 노출–균형–참여 슬라이더로 조절하고, 추천 인원과 총예산 중 하나를 필수로 만들며, 전체 폭 화면·인원/예산 한 행·영문 표제 제거·전체 목록의 규모 정보 제거
- AI 제안: 0·50·100을 노출·균형·참여 기준점으로 두고, 0→50과 50→100 사이 가중치를 선형 보간해 5단위 조절값이 실제 점수에 반영되도록 했습니다. 인원과 예산은 둘 다 입력할 수 있지만 둘 다 비면 공통 오류를 표시하는 규칙으로 해석했습니다.
- 판단: 채택. 선택지형 목적 필터보다 연속 슬라이더가 사용자의 캠페인 우선순위를 더 세밀하게 표현하고, 정확한 지표별 가중치를 함께 표시하면 조절 결과도 설명할 수 있습니다.
- 사람 검토 및 수정: 이전의 “필터당 한 행” 요구는 최신 요청에 따라 인원·예산만 한 행으로 묶는 예외로 갱신했습니다. 전체 크리에이터 목록에서는 규모 필터를 없애지 않고 각 행의 중복 규모 텍스트만 제거했습니다. 장식용 영문 표제는 삭제하고 헤더 통계는 한국어로 바꿨습니다.
- 검증: 슬라이더 양 끝의 순위 변화, 중간값을 포함한 전 구간 가중치 합, 인원·예산 공통 필수 오류와 초점 이동을 자동 테스트로 고정하고 총 34개 테스트, lint, 타입 검사, production build를 통과했습니다.
- 영향: `src/recommendation/goals.ts`, `src/recommendation/engine.ts`, `src/domain/types.ts`, `src/components/CampaignForm.tsx`, `src/components/CreatorDirectory.tsx`, `src/components/ResultsPanel.tsx`, `src/App.tsx`, 관련 테스트·스타일·문서

### AI-20260913-15 — 결과 헤더 단순화와 독립 스크롤 조건 패널

- 단계: 결과 정보 밀도와 조건 입력 조작성 개선
- 실제 요청: 결과 상단의 목적·가중치 설명 영역 제거, 결과 정렬과 후보 수 한 행 배치, 그 외 크리에이터 수 표기 통일, 추천 제목에서 규모 제거, 추천 버튼 하단 고정과 필터 독립 스크롤, 라벨의 상태 표기 제거
- AI 제안: 결과 전용 설명 툴바를 제거하고 첫 추천 섹션 헤더에 후보 수와 결과 정렬을 묶었습니다. 전체 목록도 제목과 인원수를 분리하고 정렬 선택과 한 그룹으로 맞췄습니다. 데스크톱 작업 영역은 뷰포트 높이로 제한해 왼쪽 필터와 오른쪽 결과가 각각 스크롤되게 하고, 폼 하단에 추천 버튼 전용 영역을 두었습니다.
- 판단: 채택. 점수에 사용되는 목적과 가중치는 입력 패널에서 이미 확인할 수 있어 결과 상단 반복 설명보다 후보 탐색 공간을 우선하는 편이 효율적입니다.
- 사람 검토 및 수정: `조건에 맞는 추천`에는 선택 규모를 덧붙이지 않았고, 필수 규칙은 라벨 배지가 아니라 상단 안내와 제출 오류로 유지했습니다. 좁은 화면에서도 인원과 예산은 기존 요구대로 한 행을 유지합니다.
- 검증: 후보 수·정렬의 동일 헤더 배치, 규모 없는 추천 제목, 제거된 결과 설명을 UI 테스트로 고정하고 총 34개 테스트 및 실제 데스크톱 화면에서 독립 스크롤·하단 버튼을 확인했습니다.
- 영향: `src/components/CampaignForm.tsx`, `src/components/CreatorDirectory.tsx`, `src/components/ResultsPanel.tsx`, `src/styles.css`, `src/App.test.tsx`, 관련 문서

### AI-20260913-16 — 예상 집행액과 잔여 예산 복구

- 단계: 추천 결과의 예산 의사결정 정보 보완
- 실제 요청: 제거된 사용한 예산과 남은 예산 정보 복구
- AI 제안: 제거된 목적·가중치 설명 영역 전체를 되살리지 않고, 첫 추천 섹션 설명 아래에 두 예산 값만 한 줄로 표시했습니다.
- 판단: 채택. 추천 조합이 총예산을 얼마나 사용했는지 바로 확인할 수 있으면서 결과 상단의 반복 설명은 다시 만들지 않습니다.
- 사람 검토 및 수정: 총예산을 입력하지 않으면 예산 현황을 숨기고, 비용 미상 후보가 선택된 예외 상태에서는 계산 가능한 것처럼 보이지 않도록 두 값을 `확인 필요`로 표시합니다.
- 워딩 결정: 실제 지출로 오해하지 않도록 `사용한 예산 / 남은 예산` 대신 `예상 집행액 / 잔여 예산`을 사용했습니다.
- 검증: 총예산 150만원에서 예상 집행액 30만원·잔여 예산 120만원 표시와 예산 미입력 시 미노출을 UI 테스트로 고정했습니다.
- 영향: `src/components/ResultsPanel.tsx`, `src/styles.css`, `src/App.test.tsx`, 관련 문서

### AI-20260913-17 — 플랫폼 선택 필터

- 단계: 후보 필터 확장
- 실제 요청: 플랫폼도 필터에서 선택 가능하도록 변경
- AI 제안: 기존 한 행 한 필터 구조를 유지하면서 `전체 플랫폼 / 유튜브 / 인스타그램` 단일 선택을 추가하고, 선택값을 추천 쿼리와 변경 감지에 포함했습니다.
- 판단: 채택. 원본 데이터가 두 플랫폼의 명시적 값을 제공하므로 별도 추론 없이 안전하게 후보군을 좁힐 수 있습니다.
- 검증: 플랫폼 미선택 기본 상태와 인스타그램 선택 시 해당 플랫폼 후보만 추천되는 엔진·UI 계약을 포함한 자동 테스트 36개, lint, 타입 검사와 production build를 통과했습니다.
- 영향: `src/domain/types.ts`, `src/data/creatorCsv.ts`, `src/components/CampaignForm.tsx`, `src/components/ResultsPanel.tsx`, `src/App.tsx`, 관련 테스트·문서

### AI-20260913-18 — 참여율·조회수 전용 가중치 조절

- 단계: 추천 점수 제어 범위 정교화
- 실제 요청: 참여율과 조회수만 가중치 수기 조정 대상으로 취급
- AI 제안: 슬라이더가 나누는 가중치 풀을 55%로 고정해 참여율과 평균 조회수 사이에서만 배분하고, 광고주 평점 20%·캠페인 경험 15%·예산 효율 10%는 전 구간 고정값으로 유지했습니다.
- 판단: 채택. 사용자가 조절하는 성과 목표와 시스템이 일관되게 반영하는 신뢰·경험·비용 기준을 분리해 조작 결과를 더 쉽게 설명할 수 있습니다.
- 검증: 양 끝 순위 변화, 중간값, 전 구간 합계 100%, 비조정 세 지표의 고정값과 UI 안내를 포함한 자동 테스트 36개, lint, 타입 검사와 production build를 통과했습니다.
- 영향: `src/recommendation/goals.ts`, `src/components/CampaignForm.tsx`, 추천 엔진·UI 테스트와 관련 문서

### AI-20260913-19 — 비용 미상 후보의 예산 추천 제외

- 단계: 예산 제약의 결측치 정책 강화
- 실제 요청: 평균 협업비가 없는 크리에이터를 예산 0으로 보지 말고 예산 기반 추천 대상에서 제외
- AI 제안: 총예산이 있는 정확·규모 완화 후보군에는 비용 확인 가능 후보만 유지하고, 기존 비용 미상 탐색 후보 fallback을 제거했습니다. 예산 미입력·인원 지정 추천에서는 기존처럼 비용으로 제외하지 않습니다.
- 판단: 채택. 비용을 모르는 후보는 예산 충족을 증명할 수 없으므로 예산 기반 조합의 대안으로도 노출하지 않는 것이 제약 조건에 정확합니다.
- 안전 처리: 결과 예산 합산도 `null`을 0으로 더하지 않고 모든 선택 비용이 확인됐을 때만 계산합니다.
- 검증: 비용 미상 후보만 있는 예산 쿼리가 빈 결과가 되고 기존 비용 확인 조합·예산 미입력 추천은 유지되는 계약을 포함한 자동 테스트 37개, lint, 타입 검사와 production build를 통과했습니다.
- 영향: `src/recommendation/engine.ts`, `src/domain/types.ts`, `src/components/ResultsPanel.tsx`, 추천 엔진 테스트와 관련 문서

### AI-20260913-20 — 추천 카드 수치와 슬라이더 안내 단순화

- 단계: 결과 정보 밀도와 문구 개선
- 실제 요청: 슬라이더 안내의 가중치 백분율 제거, 추천 카드의 팔로워·평균 조회수 중복 제거와 실제값 표시, `캠페인`을 `진행한 캠페인`으로 변경
- AI 제안: 슬라이더 아래에는 목적 설명 한 줄만 유지하고 접근성 설명에도 백분율을 포함하지 않았습니다. 추천 카드의 compact 근사치와 보조 실제값 구조는 천 단위 구분 실제값 하나로 통합했습니다.
- 판단: 채택. 점수 정책은 문서에 보존하면서 작업 화면에서는 의사결정에 필요한 실제 데이터와 의미가 분명한 라벨만 남깁니다.
- 검증: 목적별 접근성 설명, 가중치 안내 미노출, 추천 카드의 실제 팔로워·조회수와 `진행한 캠페인` 라벨을 포함한 자동 테스트 37개, lint, 타입 검사와 production build를 통과했습니다. 실제 브라우저 추천 카드에서도 천 단위 실제값과 새 라벨을 확인했습니다.
- 영향: `src/components/CampaignForm.tsx`, `src/components/CreatorCard.tsx`, `src/recommendation/goals.ts`, `src/styles.css`, `src/App.test.tsx`, 관련 문서
