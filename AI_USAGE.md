# AI 활용 기록

이 문서는 모비데이즈 P&A 사전 과제를 진행하며 AI를 어떤 목적으로 사용했고, 제안을 어떤 기준으로 검토·수정했는지 정리합니다. 시간순 작업 일지보다 최종 결과에 영향을 준 입력, 판단, 검증 과정을 주제별로 기록했습니다.

## 1. 사용 도구

### OpenAI Codex

다음 작업에 사용했습니다.

- 과제 안내문의 필수 기능, 가산점, 제출 형식 구조화
- CSV 데이터 프로파일링과 결측치·경계값·중복 확인
- 제품 범위, 추천 정책, 예외 처리와 작업 순서 설계
- 데이터·추천, UX·프론트엔드, 문서, QA 역할별 검토 분배
- React·TypeScript 소스 구현과 제출 문서 작성
- 단위·UI 테스트, 정적 검사, production build와 브라우저 검증
- 구현 변경에 따른 README·PRD·flowchart·AI 활용 기록 동기화

AI는 초안 생성과 반복 검토에 사용했으며, 추천 정책과 사용자 경험에 영향을 주는 결정은 데이터와 실제 동작을 확인한 뒤 사람이 확정했습니다.

## 2. 제공한 컨텍스트

- 모비데이즈 P&A 사전 과제 안내 Notion 페이지
- 과제에서 제공한 `dummy_creators.csv`
- 프로젝트 루트의 `AGENTS.md`
- 전체 계획 `PROJECT_PLAN.md`
- 대화 간 진행 기록 `PROJECT_STATUS.md`
- 구현 중 사용자가 전달한 화면 캡처와 UX 수정 요청
- 현재 소스, 테스트 결과와 제출 문서

개인정보, 비밀키, 환경변수는 AI 컨텍스트로 제공하지 않았습니다. 원본 CSV는 읽기 전용 기준 데이터로 취급하고 애플리케이션용 사본과 Git blob이 동일한지 별도로 검증했습니다.

## 3. 주요 프롬프트와 활용 목적

### 요구사항 파악과 전체 계획

> 모비데이즈라는 회사에 지원해서 서류 합격했어. 과제전형을 진행해야 해. 아래 과제 안내 문서의 내용을 우선 완벽하게 파악해.

> 요구하는 최고 수준으로 이 과제를 수행하기 위한 작업 순서를 계획해.

활용 결과:

- 필수 기능, 가산점, 제출 방식과 문서 요구사항을 분리했습니다.
- 화면 구현보다 추천 로직의 설계 근거와 문서화가 핵심 평가 대상임을 확인했습니다.
- 데이터 분석 → 추천 정책 → UX 상태 → 구현 → 검증 → 문서 → 제출 순서로 작업했습니다.
- 추천 엔진을 UI와 분리된 순수 함수로 먼저 설계해 테스트 가능성을 확보했습니다.

### 범위와 협업 구조 재검토

> 제한시간은 72시간이지만 나는 앞으로 7시간 동안 과제를 진행할 거야. 계획에 이 점을 반영해.

> 일단 시간은 신경쓰지 말고 방금 풀버전 계획을 사용하자. 빨리 작업하면 되니까.

> 작업 순서에 따른 최적의 대화 및 에이전트 작업 배분안도 함께 작성해.

활용 결과:

- 처음에는 7시간용 축소안을 만들었지만, 사용자 판단에 따라 품질 중심의 전체 계획으로 복귀했습니다.
- 시간 제한을 이유로 필수 문서, 추천 근거, 테스트와 예외 처리를 제거하지 않았습니다.
- 총괄, 데이터·추천, UX·프론트엔드, 문서, QA 역할을 분리하고 보고 결과를 총괄 작업에서 원본과 대조했습니다.
- 프로젝트 대화에서 로컬 문서를 직접 읽을 수 없는 문제는 공통 Sources에 문서와 CSV를 동기화해 해결했습니다.

### 데이터 분석과 원본 무결성

AI에 CSV의 스키마, 수치 범위, 플랫폼·카테고리·팔로워 구간 분포, 중복과 결측치 관계를 분석하도록 요청했습니다. 분석 결과를 그대로 신뢰하지 않고 재현 가능한 Node.js 프로파일러를 추가해 다시 계산했습니다.

활용 결과:

- 200행·11열, 10개 카테고리, 유튜브 106명·인스타그램 94명을 확인했습니다.
- 팔로워 기준을 나노 1만 미만, 마이크로 1만 이상 10만 미만, 매크로 10만 이상으로 정의했습니다.
- 광고주 평점 누락 27건과 캠페인 이력 0건이 같은 행에 있음을 확인했습니다.
- 중복 ID·이름과 유효하지 않은 무이력 행이 없음을 확인했습니다.
- Git 줄바꿈 자동 정규화로 원본 해시가 바뀌는 문제를 발견해 `.gitattributes`에서 CSV를 `-text`로 지정하고 원본과 Git blob의 SHA-256 일치를 재확인했습니다.

### 추천 수식과 예산 최적화

> 총 예산을 단순히 인원수로 나누지 말고, 예산 안에서 조건에 가장 적합한 조합을 짜줘.

> 총 예산이나 인당 예산 중 하나를 선택해서 입력할 수 있게 해.

AI가 제안한 여러 정규화·예산 효율 수식을 비교하고 실제 데이터에서 순위와 경계 사례를 검산했습니다.

활용 결과:

- 참여율·평균 조회수·보정 평점은 동일 팔로워 규모 내 평균순위 백분위로 정규화했습니다.
- 평점은 캠페인 수를 신뢰도로 사용한 Bayesian 보정값, 경험은 `log1p` 비율을 사용했습니다.
- 예산을 입력하면 동일 규모 내 비용 백분위 역값을 예산 효율로 사용하고, 예산이 없으면 모든 후보에 중립값 `0.5`를 적용했습니다.
- 총예산은 평균 협업비 합이 예산 이하인 조합 중 반올림 전 추천 점수 합이 가장 큰 조합을 0/1 배낭 동적 계획법으로 선택했습니다.
- 추천 인원이 있으면 해당 인원수의 조합만 비교하고, 없으면 가능한 모든 인원수에서 최적 조합을 찾습니다.
- 1인당 예산은 후보별 평균 협업비 상한으로 사용하고, 추천 인원을 비우면 상한 이하 전체 후보를 반환합니다.
- 동점은 낮은 예상 비용, 크리에이터 ID 순으로 결정해 결과를 재현 가능하게 했습니다.

### 목적 가중치와 필터 UX

> 캠페인 목적 필터를 노출-균형-참여 slider로 두고, 가중치 수준을 사용자가 조절할 수 있도록 해줘.

> 참여율, 조회수만 가중치 수기 조정 대상으로 취급해.

> 크리에이터 규모와 추천 인원 필터를 한 행에 붙여. 마이크로 3명 등의 사고가 가능하게.

활용 결과:

- 슬라이더의 0·50·100을 노출 확대·균형 탐색·참여 중심 기준점으로 정의했습니다.
- 사용자가 조절하는 지표는 참여율과 평균 조회수뿐이며 합계 55% 안에서 선형 보간합니다.
- 광고주 평점 20%, 캠페인 경험 15%, 예산 효율 10%는 전 구간에서 고정했습니다.
- 추천 인원 또는 예산 중 하나는 필수이며, 총예산과 1인당 예산은 상호배타적으로 선택하게 했습니다.
- 플랫폼·카테고리·규모는 선택 필터로 유지하고, 규모와 추천 인원을 같은 행에 배치했습니다.
- 추천 버튼은 필터 패널 하단에 두고 데스크톱에서 필터와 결과가 독립적으로 스크롤되게 했습니다.
- 장식용 영문 표제와 필터를 반복하는 결과 요약을 제거하고, 추천 카드에는 실제 팔로워·조회수와 자연스러운 한국어 라벨만 남겼습니다.

### 전체 목록과 결과 설명

> 추천받기 전에는 몇 가지 기본 정렬로 전체 크리에이터가 노출되고, 추천받기 버튼을 누르면 재정렬해줘.

> 사용한 예산/남은 예산은 다른 워딩을 추천해줘.

활용 결과:

- 추천 전에는 전체 200명을 팔로워순으로 표시하고 조회수·참여율·평점·협업비 정렬을 제공합니다.
- 추천 후에는 선택된 후보를 상단 카드로 보여주고 나머지 전체 목록을 유지합니다.
- 추천 카드는 핵심 기여 지표와 조건 완화 여부를 실제 계산값에서 설명합니다.
- 실제 지출로 오해하지 않도록 총예산 결과에 `예상 집행액`과 `잔여 예산`을 사용했습니다.
- 1인당 예산 결과에는 `1인당 예산 상한`과 `후보 중 최고 예상 협업비`를 표시합니다.
- 결과가 없을 때 전체 목록과 예산·규모·플랫폼·카테고리 수정 동작을 함께 제공합니다.

### 구현과 QA 요청

추천 데이터 계약, 파서, 점수 계산, 후보 선택, fallback과 근거 생성을 UI와 분리하고 경계·결측·불변성 테스트를 함께 작성하도록 요청했습니다. UI 구현 후에는 자동 검사와 실제 브라우저 검증을 반복했습니다.

활용 결과:

- 잘못된 CSV 행은 해당 행만 제외하고, 필수 헤더 누락이나 유효 행 0건은 재시도 가능한 오류로 처리했습니다.
- 테스트 간 mock 호출 수가 공유되던 실패는 각 테스트에서 상태를 초기화해 해결했습니다.
- 보안 감사에서 보고된 테스트 도구 의존성은 승인된 범위에서 업데이트하고 전체 검사를 다시 수행했습니다.
- 사용자 수정 요청마다 추천 엔진, UI 테스트와 제출 문서를 함께 갱신했습니다.

## 4. AI 결과를 그대로 사용하지 않은 판단 사례

AI의 첫 결과를 완성본으로 간주하지 않았습니다. 실행 가능한 결과가 나온 뒤에도 데이터의 의미, 광고주의 의사결정 방식, 화면의 정보 밀도와 과제 평가 기준을 다시 확인하고 필요한 경우 명시적으로 “이 방향은 아니다”라고 판단해 재요청했습니다.

### 사례 1. 시간에 맞춘 축소안보다 평가 기준을 선택

- **AI 초기안:** 실제 작업 가능 시간이 7시간이라는 조건에 맞춰 기능과 문서 범위를 줄인 실행안을 제안했습니다.
- **문제 판단:** 이 과제는 화면의 완성도뿐 아니라 추천 논리, 예외 처리, AI 활용 과정과 문서화를 평가합니다. 시간을 이유로 이 항목을 줄이면 구현은 빨라져도 평가 근거가 약해진다고 판단했습니다.
- **재요청·수정:** “시간은 신경 쓰지 말고 풀버전 계획을 사용하자”고 방향을 바꾸고, 필수 문서·추천 근거·테스트·fallback을 유지했습니다.
- **최종 결과:** 데이터 분석부터 추천 정책, 구현, 브라우저 QA와 제출 문서까지 이어지는 전체 계획을 실행했습니다. AI를 일정 단축 도구로만 사용하지 않고 평가 목적에 맞춰 범위를 다시 통제한 사례입니다.

### 사례 2. 추천 전 빈 화면을 전체 탐색 화면으로 변경

- **AI 초기안:** 사용자가 조건을 입력하고 `크리에이터 추천받기`를 눌러야 후보 목록이 처음 표시됐습니다.
- **문제 판단:** 평가자와 사용자가 원본 데이터의 범위와 품질을 미리 확인할 수 없고, 추천 결과 외의 후보를 비교하기도 어려웠습니다.
- **재요청·수정:** 추천 전에는 전체 200명을 기본 지표순으로 노출하고, 추천 실행은 목록을 없애는 동작이 아니라 적합 후보를 상단에 재배치하는 동작으로 정의했습니다.
- **최종 결과:** 초기에는 팔로워·조회수·참여율·평점·협업비 정렬이 가능한 전체 목록을, 추천 후에는 추천 카드와 나머지 후보 목록을 함께 제공합니다. 알고리즘 결과만 보여주는 화면을 데이터 탐색과 비교가 가능한 도구로 바꿨습니다.

### 사례 3. 총예산 균등 배분을 조합 최적화로 교체

- **AI 초기안:** 총예산을 추천 인원으로 나눈 `1인당 기준 예산`과 각 크리에이터의 평균 협업비를 비교했습니다.
- **문제 판단:** 균등 분배는 구현과 설명은 쉽지만, 고성과·고비용 후보와 효율적인 저비용 후보를 함께 선택할 수 없습니다. 사용자가 입력한 것은 개인별 동일 상한이 아니라 전체 캠페인 예산이므로 실제 의사결정 문제와 달랐습니다.
- **재요청·수정:** 총예산을 단순히 인원수로 나누지 말고 예산 안에서 가장 적합한 조합을 찾도록 재요청했습니다. 이후 개인별 상한도 필요할 수 있어 총예산과 1인당 예산을 서로 다른 선택형 제약으로 분리했습니다.
- **최종 결과:** 총예산은 0/1 배낭 동적 계획법으로 점수 합이 가장 높은 조합을 선택하고, 1인당 예산은 개별 평균 협업비 상한으로 적용합니다. 입력 의미와 알고리즘의 목적함수를 일치시켰습니다.

### 사례 4. 결측 데이터를 무료·저평가로 해석하지 않음

- **AI 초기안:** 캠페인 이력이 없는 행의 평균 협업비 0원을 실제 0원처럼 비교하거나 별도 `탐색 후보`로 추천하고, 누락 평점은 단순 기본값으로 치환할 수 있었습니다.
- **문제 판단:** 비용 미상 후보를 무료 후보로 추천하면 예산 준수를 증명할 수 없고, 평점 공란을 0점으로 바꾸면 평가받지 않은 후보를 낮은 평가 후보로 왜곡합니다.
- **재요청·수정:** 평균 협업비가 없는 후보는 “예산 조건으로 추천할 때만 빠지는가, 아니면 전체 목록에서도 사라지는가”를 다시 확인하고 전자만 허용했습니다. 평점은 캠페인 수를 신뢰도로 사용한 Bayesian 보정값으로 처리했습니다.
- **최종 결과:** 비용 미상 후보는 예산 기반 추천에서는 제외하지만 전체 목록에는 `견적 확인 필요`로 유지하고, 예산이 없으면 다른 성과 지표로 평가합니다. 누락 평점은 `이력 없음`으로 표시해 원본 데이터의 의미를 보존했습니다.

### 사례 5. 고정 목적 선택지를 제어 가능한 슬라이더로 변경

- **AI 초기안:** 노출·참여·전환·균형 네 가지 목적에 고정 가중치를 부여했습니다.
- **문제 판단:** 제공 데이터에는 직접적인 전환 지표가 없어 `전환 중심`의 근거가 약하고, 네 가지 프리셋만으로는 광고주의 우선순위를 세밀하게 표현하기 어려웠습니다. 모든 지표를 사용자가 조절하면 오히려 판단 부담과 점수 왜곡 가능성이 커집니다.
- **재요청·수정:** 목적을 노출–균형–참여 슬라이더로 바꾸고, 사용자가 조절하는 대상은 참여율과 평균 조회수만으로 제한했습니다.
- **최종 결과:** 두 성과 지표는 합계 55% 안에서 선형 보간하고 평점 20%·경험 15%·예산 10%는 고정했습니다. 조절 가능성과 추천 안정성 사이의 경계를 명확히 했습니다.

### 사례 6. 예산을 넘기는 fallback을 거절

- **AI 초기안:** 정확 후보가 없으면 카테고리를 유지한 채 예산을 제한적으로 초과하고, 이후 인접 팔로워 규모를 탐색하는 순서를 제안했습니다.
- **문제 판단:** “예산 안에서 가장 적합한 조합”이라는 명시적 요구와 예산 초과 추천은 양립할 수 없습니다. 후보를 억지로 채우는 것보다 제약을 지키는 편이 추천 신뢰도에 중요하다고 판단했습니다.
- **재요청·수정:** 기존 예산 초과 fallback과 비용 미상 탐색 후보를 제거하고, 플랫폼·카테고리·예산은 유지한 채 인접 규모만 확인하도록 범위를 제한했습니다.
- **최종 결과:** 예산은 자동으로 초과하지 않습니다. 후보가 없으면 빈 결과와 조건 수정 동작을 제공하고, 규모가 완화됐을 때만 그 사실을 결과에 표시합니다.

### 사례 7. AI가 추가한 정보를 화면에서 반복 제거

- **AI 초기안:** 결과 상단에 적용 조건과 목적별 가중치를 다시 표시하고, 영문 표제와 크리에이터 수치의 근사치·실제값을 함께 노출했습니다.
- **문제 판단:** 필터에서 이미 확인한 내용을 결과에서 반복하면 핵심 후보보다 설명이 먼저 보입니다. 영문 장식과 중복 수치도 의사결정에 기여하지 않았습니다. 반대로 예산 사용 현황까지 함께 제거하자 조합의 실용성을 판단할 정보가 부족해졌습니다.
- **재요청·수정:** 중복 조건, 가중치 백분율, 영문 타이틀과 수치 중복은 삭제하고, 추천 버튼을 하단에 고정해 필터 조작성을 높였습니다. 이후 예산 정보만 `예상 집행액·잔여 예산` 또는 1인당 상한 형태로 선택적으로 복구했습니다.
- **최종 결과:** 화면에는 필터, 후보 비교, 추천 근거와 예산 판단에 필요한 정보만 남았습니다. AI 결과를 일괄 수용하거나 일괄 삭제하지 않고 각 정보의 의사결정 가치를 기준으로 편집했습니다.

### 사례 8. 문구의 논리적 모호함을 그대로 두지 않음

- **AI 초기안:** 예산 입력 안내에 `비우려면 추천 인원을 입력해 주세요`라는 문구를 사용했습니다.
- **문제 판단:** 무엇을 비운다는 뜻인지 대상이 생략돼 있고, 사용자가 수행해야 할 행동보다 내부 유효성 규칙처럼 읽혔습니다. 빈 결과 제목도 카테고리를 선택하지 않은 경우까지 `카테고리를 유지한 대안`이라고 단정했습니다.
- **재요청·수정:** 문구의 의미가 적절한지 먼저 질문해 논리를 확인한 뒤, `입력하지 않으면 예산 제한 없이 추천해요`와 `조건에 맞는 추천 후보를 찾지 못했어요`처럼 현재 상태와 다음 행동이 직접 드러나게 수정했습니다.
- **최종 결과:** 필수 규칙은 폼 상단과 제출 오류에서 전달하고, 개별 안내와 빈 결과는 선택 여부와 무관하게 정확히 읽히도록 정리했습니다. 작은 문구도 기능 계약의 일부로 보고 검증한 사례입니다.

## 5. 검증 방식

### 데이터와 추천 정책

- `node scripts/profile-data.mjs`로 200행·11열, 플랫폼·카테고리·규모 분포, 결측 27건과 중복 0건을 재현했습니다.
- 제공 CSV, 애플리케이션 사본과 현재 Git blob의 SHA-256이 일치하는지 확인했습니다.
- 나노·마이크로·매크로 경계와 평점·비용 결측치를 고정 테스트로 검증했습니다.
- 총예산 최적 조합, 1인당 예산 상한, 비용 미상 제외, 인원 지정·미지정과 결정론적 동점 처리를 검증했습니다.
- 슬라이더 양 끝과 중간값에서 참여율·조회수 순위가 바뀌고 전체 가중치 합이 항상 100%인지 확인했습니다.

### 코드와 UI

- Vitest와 Testing Library 자동 테스트 41개를 통과했습니다.
- ESLint와 TypeScript 타입 검사를 통과했습니다.
- Vite production build를 통과했습니다.
- 추천 전 200명 탐색, 추천 후 상단 후보와 나머지 목록 유지, 빈 결과와 재시도 흐름을 확인했습니다.
- 데스크톱과 390px 모바일 폭에서 레이아웃, 독립 스크롤, 하단 추천 버튼을 확인했습니다.
- 입력 오류 시 초점 이동, 접근성 이름, 키보드 조작과 로딩 중 입력 비활성화를 테스트했습니다.

### 의존성과 문서

- `npm audit` 결과 취약점 0건을 확인했습니다.
- README·PRD·flowchart와 구현의 팔로워 구간, 가중치, 예산, 비용 미상과 fallback 정책을 대조했습니다.
- 공개 저장소의 필수 문서와 커밋 이력이 유지되고 로컬 `main`과 원격 `main`이 일치하는지 확인했습니다.

## 6. AI 활용 방식과 역량

### 문제를 역할과 산출물 단위로 분해

AI에 한 번에 완성품을 요청하지 않고 요구사항 분석, 데이터 검증, 추천 모델, UX, 구현, 문서와 QA로 문제를 나눴습니다. 역할별 대화에는 공통 계획과 원본 데이터를 제공하고, 서로 다른 제안은 총괄 작업에서 데이터·평가 기준과 대조해 하나의 정책으로 통합했습니다. 이를 통해 병렬 작업의 속도는 활용하되 서로 다른 역할이 만든 기준이 제품에 혼재하지 않도록 관리했습니다.

### 자연어 요구를 검증 가능한 계약으로 변환

`적합한 크리에이터`, `균형`, `예산 안에서` 같은 표현을 그대로 구현하지 않고 팔로워 구간, 정규화 방식, 가중치 합, 예산 제약식, 동점 처리와 fallback 순서로 구체화했습니다. 결정은 README·PRD·flowchart에 기록하고 같은 계약을 단위 테스트로 고정했습니다. 프롬프트를 코드 생성 명령이 아니라 제품 정책을 명료화하는 도구로 사용했습니다.

### Human-in-the-loop 반복 개선

각 버전을 실제 화면에서 확인하고 정보 중복, 입력 순서와 문구의 모호함을 다시 AI에 전달했습니다. 수정 범위를 구체적으로 제한한 뒤 UI만 바뀌는 요청도 추천 정책·테스트·문서에 영향이 있는지 함께 점검했습니다. AI의 제안은 채택·부분 채택·거절로 구분했고, 폐기한 안은 이유와 최종 결정을 남겨 같은 문제가 되풀이되지 않게 했습니다.

### 결과를 서로 다른 증거로 교차검증

AI가 분석한 데이터 통계는 재현 가능한 프로파일러로 다시 계산하고 원본 CSV와 Git blob의 해시를 비교했습니다. 구현 결과는 자동 테스트 41개, lint, 타입 검사, production build와 브라우저 확인으로 검증했습니다. 테스트가 실패했을 때 기능 코드를 우회하지 않고 mock 격리 문제를 찾아 수정했으며, 의존성 보안 감사도 별도로 수행했습니다.

### 작은 변경과 추적 가능한 이력 유지

요구사항을 적절한 작업 단위로 나눠 구현·검증 후 각각 커밋하고 원격 저장소에 반영했습니다. 추천 정책이 바뀌면 코드뿐 아니라 테스트와 제출 문서를 함께 갱신해 설명과 실제 동작이 어긋나지 않게 했습니다. 이 방식으로 AI가 빠르게 만든 결과의 품질을 사람이 단계별로 확인하고, 문제가 생겨도 변경 원인을 추적할 수 있게 했습니다.

AI에게 최종 판단을 위임하지 않았습니다. 대안 생성, 구현, 검산과 반복 수정에는 적극적으로 활용하되, 어떤 문제를 풀 것인지와 어느 제약을 지킬 것인지는 데이터, 사용자 경험과 평가 목적을 근거로 직접 결정했습니다.

## 7. 최종 상태와 이후 확인할 내용

현재 버전은 제공 CSV만으로 실행되는 브라우저 기반 프로토타입이며, 별도 서버·외부 API·비밀키를 사용하지 않습니다. 추천 전 전체 탐색, 목적 가중치 조절, 총예산 조합 최적화, 1인당 예산 상한, 설명 가능한 추천과 빈 결과 복구 흐름을 구현했습니다.

제출 후 실제 서비스로 확장한다면 다음 내용을 추가로 확인합니다.

- 배포 환경과 최종 공개 URL
- 실제 견적과 캠페인 성과를 이용한 가중치 보정
- 실제 사용자 피드백에 따른 문구와 조건 입력 흐름 개선
- 추천 결과 저장·공유와 운영 중 데이터 품질 모니터링
