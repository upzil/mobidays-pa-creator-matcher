# Creator Match 추천 흐름

아래 흐름은 입력부터 원본 데이터 검증, 점수 계산, 정확 추천, 탐색 후보, 조건 완화, 정렬과 회복 행동까지 실제 구현 계약을 나타냅니다.

```mermaid
flowchart TD
    Z[앱 진입] --> C[UTF-8 BOM 제거 후 CSV 파싱]
    C --> D{필수 헤더와 파일 검증}
    D -->|로드 실패·헤더 오류| D1[치명적 데이터 오류와 다시 불러오기]
    D -->|통과| E[행별 타입·교차 조건 검증]
    E --> F{유효 행이 있는가}
    F -->|없음| D1
    F -->|있음| G[오류 행 격리와 진단 정보 보존]
    G --> H[팔로워 규모 분류]
    H --> X[전체 크리에이터를 팔로워순으로 표시]
    X --> X1{기본 정렬 변경}
    X1 -->|예| X2[조회수·참여율·평점·협업비로 전체 목록 재정렬]
    X1 -->|아니오| A[추천 인원·총예산과 플랫폼·카테고리·규모 설정]
    X2 --> A

    A --> B{추천 인원·총예산 중 하나가 있고 값이 유효한가}
    B -->|실패| B1[필드별 오류 표시 후 첫 오류에 초점]
    B1 --> A
    B -->|통과| I0[노출–균형–참여 슬라이더 가중치 보간]
    I0 --> I[규모 전체 모집단 기준 성과 지표 계산]
    I --> I1[참여율·조회수 평균순위 백분위]
    I --> I2[Bayesian 보정 평점 백분위]
    I --> I3[log1p 캠페인 경험]

    I1 --> J[선택한 필터만 적용해 후보 분류]
    I2 --> J
    I3 --> J
    J --> K[비용 백분위·목적별 가중 원점수·추천 이유 계산]
    K --> L{총예산을 입력했는가}
    L -->|아니오| M[추천 인원 입력 시 상위 N명·미입력 시 전체 후보]
    L -->|예| N[비용 확인 후보로 Pareto frontier 동적 계획법 실행]
    N --> O{총예산 안에서 정확 조합이 있는가}
    O -->|예| O1[원점수 합 최대 조합 선택]
    O -->|아니오| Q{인접 규모에 총예산 이내 조합이 있는가}
    Q -->|예| Q1[규모 완화 조합과 완화 사실 표시]
    Q -->|아니오| R{같은 카테고리·규모 탐색 후보가 있는가}
    R -->|예| R1[비용·평점 확인 필요 표시]
    R -->|아니오| S[완전 빈 결과와 조건 수정 행동]

    M --> T[추천 카드·후보 수·정렬 UI]
    O1 --> EB{선택 후보의 협업비를 모두 아는가}
    Q1 --> EB
    R1 --> EB
    EB -->|예| EB1[예상 집행액·잔여 예산 계산]
    EB -->|아니오| EB2[예산 확인 필요 표시]
    EB1 --> T
    EB2 --> T
    T --> Y[추천 후보를 상단에 배치하고 나머지 전체 목록 유지]
    Y --> U{사용자 행동}
    U -->|정렬 변경| V[등급 섹션 내부에서 안정 정렬]
    V --> T
    U -->|조건 편집| W[기존 결과 유지·변경 안내]
    W -->|다시 추천| B
    S --> Y1[전체 크리에이터 목록 유지]
    Y1 --> U
    S -->|필드 수정 선택| A
```

## 계산과 분기 원칙

- 모든 성과 정규화는 현재 쿼리 후보가 아니라 전체 유효 데이터의 실제 팔로워 구간을 기준으로 합니다.
- 추천 전에는 200명 전체를 팔로워 많은 순으로 표시하며 조회수·참여율·평점·협업비 정렬도 제공합니다. 결측 평점과 비용은 해당 정렬에서 마지막에 둡니다.
- 추천 후에는 추천 후보를 상단 상세 영역으로 올리고 추천되지 않은 나머지 크리에이터를 아래 목록에 유지해, 항상 전체 200명을 탐색할 수 있게 합니다.
- 캠페인 목적은 노출–균형–참여의 0~100 슬라이더이며 `균형 탐색` 50이 기본값입니다. 추천 인원은 입력하면 1~20명 범위만 허용하고 비워두면 인원수 제약을 두지 않습니다.
- 추천 인원과 총예산 중 하나는 필수입니다. 총예산은 만원 단위이며 선택 후보의 평균 협업비 합이 이를 넘지 않아야 합니다. 예산 미입력은 비용 제한 없음과 예산 기여도 `0.5`, 플랫폼·카테고리·규모 미선택은 각 항목의 전체 범위를 뜻합니다.
- 플랫폼을 선택하면 해당 플랫폼 안에서만 카테고리·규모 조건과 추천 점수를 적용합니다.
- 규모를 선택하지 않아도 성과 지표는 각 후보의 실제 팔로워 구간을 모집단으로 정규화합니다.
- 평균순위 백분위는 `(L + (E + 1) / 2 - 1) / (N - 1)`이며 한 행뿐인 모집단은 `0.5`입니다.
- 보정 평점은 `(campaignCount * rating + 5 * 4.415028901734104) / (campaignCount + 5)`입니다.
- 경험은 `log(1 + campaignCount) / log(1 + segmentMaxCampaignCount)`, 예산 효율은 `1 - 동일 규모 내 비용 백분위`입니다.
- 총예산이 있으면 0/1 배낭 문제로 예상 비용 합이 예산 이하인 조합 중 반올림 전 원점수 합이 가장 큰 조합을 선택합니다. 추천 인원을 입력하면 해당 개수만, 비우면 가능한 모든 개수를 비교합니다.
- 최종 점수는 슬라이더 위치의 가중치를 사용합니다. 노출 0은 조회수 45%, 균형 50은 참여율 30%, 참여 100은 참여율 45%를 가장 크게 반영하며 중간값은 양쪽 기준을 선형 보간합니다. 정확한 비중은 결과 화면에 공개하고 반올림 전 원점수로 정렬합니다.
- 카테고리는 fallback에서도 유지합니다.
- 정확 조합이 없을 때 인접 규모, 비용 미상 탐색 후보 순으로 처음 발견된 한 단계만 표시하며 예산을 자동으로 초과하지 않습니다.
- 마이크로의 인접 규모는 나노와 매크로 모두이며, 나노·매크로의 인접 규모는 마이크로입니다.
- 비용과 평점이 없는 탐색 후보는 중립 기여도를 사용하되 실제 값처럼 표시하지 않습니다.
- 비용 미상 후보는 총예산 충족을 보장할 수 없어 비용 확인 가능 조합과 섞지 않습니다.
- 결과 영역에는 입력 패널과 중복되는 적용 조건 요약을 반복하지 않고, 목적별 가중치·추천 섹션·카드부터 표시합니다.
- 어떤 조건도 사용자에게 알리지 않고 자동으로 바꾸지 않습니다.
