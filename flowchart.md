# Creator Match 추천 흐름

아래 흐름은 입력부터 정확 일치 추천, 결측치 처리, 조건 완화, 결과 정렬까지 포함합니다.

```mermaid
flowchart TD
    A[사용자 조건 입력] --> B{입력 유효성 검사}
    B -->|실패| B1[필드별 오류와 수정 방법 표시]
    B1 --> A
    B -->|통과| C[원본 CSV 로드]

    C --> D{스키마와 행 검증}
    D -->|전체 로드 실패| D1[데이터 오류와 다시 시도 안내]
    D -->|일부 행 오류| D2[오류 행을 안전하게 제외하고 기록]
    D -->|정상| E[도메인 데이터 변환]
    D2 --> E

    E --> F[팔로워 규모 분류]
    F --> G[규모별 지표 정규화]
    G --> H[선택 카테고리와 규모 필터]
    H --> I{캠페인 이력 여부}

    I -->|있음| J{평균 협업비가 예산 이내인가}
    J -->|예| K[정확 일치 후보]
    J -->|아니오| L[예산 초과 후보로 보관]
    I -->|없음| M[비용 미상 탐색 후보로 보관]

    K --> N{정확 일치 후보가 있는가}
    N -->|예| O[가중 점수 계산]
    O --> P[실제 계산값으로 추천 근거 생성]
    P --> Q[결정론적 순위와 기본 정렬]
    Q --> R[적용 조건·추천 카드·정렬 UI 표시]

    N -->|아니오| S{같은 카테고리·규모에서 예산 120% 이내 후보가 있는가}
    S -->|예| S1[예산 완화 후보와 초과 폭 표시]
    S1 --> O
    S -->|아니오| T{같은 카테고리의 인접 규모 중 예산 이내 후보가 있는가}
    T -->|예| T1[규모 완화 후보와 변경 내용 표시]
    T1 --> O
    T -->|아니오| U{비용 미상 탐색 후보가 있는가}
    U -->|예| U1[비용·평점 확인 필요를 명시]
    U1 --> O
    U -->|아니오| V[후보가 없는 이유와 조건 수정 행동 표시]
    V --> A

    R --> W{사용자 행동}
    W -->|정렬 변경| X[선택 기준으로 안정 정렬]
    X --> R
    W -->|조건 수정| A
```

## 주요 분기 원칙

- 카테고리는 추천 관련성의 핵심이므로 fallback에서도 유지합니다.
- 예산 조건을 먼저 완화하고, 그다음 인접 팔로워 규모를 검토합니다.
- 캠페인 이력이 없는 후보는 비용과 만족도를 알 수 없는 탐색 후보입니다.
- 어떤 조건도 사용자에게 알리지 않고 자동으로 완화하지 않습니다.
- 추천 근거는 필터 결과와 계산 지표에서 생성합니다.
